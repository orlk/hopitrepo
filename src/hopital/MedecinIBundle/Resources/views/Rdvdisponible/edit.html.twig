{% extends 'MedecinIBundle::CalendrierM.html.twig' %}

{% block contenent %}
    <head>
<link  rel="stylesheet" type="text/css"  media="all" href={{asset('web/css/style.css')}} />
</head>
 <h1 class="titre_d">Votre calendrier </h1>
   <form action="" method="POST">
            <table >
                <tr>
                    <td>{{form_label(edit_form.dater, "Date disponible: ")}}</td>
                    <td>{{form_widget(edit_form.dater)}}</td>
                </tr>
                <tr>
                    <td>{{form_label(edit_form.heurer, "Heure disponible: ")}}</td>
                    <td>{{form_widget(edit_form.heurer)}}</td>
                </tr>
                
                <tr><td>{{ form_widget(edit_form.Valider, { 'attr': {'class': 'bttn'} })}}</td>
                 <td class="bttnL">   
                 
  
        <a href="{{ path('rdvdisponible_show', { 'id': entity.id }) }}">
            Back
        </a>
   </td></tr>
                <tr>
                    <td>{{form_widget(edit_form.cinm,{'attr': { 'value' : app.user.id   } })}}</td>
                </tr>
             {{ form_rest(edit_form)}}
            </table>
        </form>
{% endblock %}
