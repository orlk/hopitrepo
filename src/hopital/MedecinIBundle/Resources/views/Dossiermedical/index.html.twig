{% extends 'MedecinIBundle::DossierMedical.html.twig' %}

{% block contenent %}
    <head>
<link  rel="stylesheet" type="text/css"  media="all" href={{asset('web/css/style.css')}} />
</head>
    <h1 class="titre_d">liste des dossiers medicaux</h1>

    <table>
        <thead>
             <tr> <th>
            <form action="" method="POST">
                Code de patient:  <input type="text" name="search"/>
                  </th>
                  <th  > <input class="bttn" type="submit" value="Recherche" ></th>
</form>     
                            
           </tr>
            <tr>
                <th >Code de dossier</th>
                <th >Date de creation</th>
                 <th>Cin de Patient</th>
             
            </tr>
           
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td >{{ entity.codedm }}</td>
                <td >{% if entity.datecreation %}{{ entity.datecreation|date('Y-m-d H:i:s') }}{% endif %}</td>
                 <td > {{entity.codepatient.cin}}</td>
                <td >
                <ul>
                    <li>
                      	<div class="bttnL"><span> <a href="{{ path('dossiermedical_show', { 'codeDm': entity.codedm }) }}">show</a></span></div>
                    </li>
                   
                </ul>
                </td>
            </tr>
        {% endfor %}
        <tr>
            <td class="bttnL">   <a href="{{ path('dossiermedical_new') }}">
                Nouveau dossier
            </a></td>
        </tr>
        </tbody>
    </table>

   {% endblock %}
